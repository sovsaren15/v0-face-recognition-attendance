{
  "name": "my-v0-project",
  "version": "0.1.0",
  "packageManager": "pnpm@8.6.1",
  "engines": {
    "node": "22.x"
  },
  "private": true,
  "scripts": {
    "install": "pnpm -r install",
    "dev": "pnpm --filter smart-attendance-frontend start",
    "start:backend": "node backend/server.js",
    "build": "pnpm --filter smart-attendance-frontend build"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "typescript": "^5"
  }
}